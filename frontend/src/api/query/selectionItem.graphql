query selectionItem($id: GraphQLObjectId!) {
  selection {
    item(id: $id) {
      id
      title
      text
      images
      relevanceDate
      categories {
        title
        slug
      }
      brands {
        title
      }
      shops {
        title
      }
    }
  }
}
