query selectionList($filter: SelectionFilterInput) {
  selection {
    list(filter: $filter) {
      id
      title
      text
      images
      createdAt
      brands {
        id
        title
      }
      categories {
        id
        title
      }
      shops {
        id
        title
      }
    }
  }
}
