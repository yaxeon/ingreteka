query selectionItem($id: GraphQLObjectId!) {
  selection {
    item(id: $id) {
      id
      title
      text
      images
      isPublished
      relevanceDate
      createdAt
      brands {
        id
        title
      }
      categories {
        id
        title
      }
      shops {
        id
        title
      }
    }
  }
}
