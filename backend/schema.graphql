directive @auth(roles: [String]) on FIELD_DEFINITION

enum UserRole {
  ADMIN
  USER
}

type User {
  username: String!
  picture: String!
  roles: [UserRole]!
}

type Query {
  profile: User @auth(roles: ["USER"])
}
